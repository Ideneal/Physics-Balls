This is a physical design that describes the physical movement of some balls.
Was created with the use of the SDL libraries and written in C++ OOP

For working install SDL libraries:
- apt-get install libsdl1.2-dev libsdl-image1.2-dev libsdl-mixer1.2-dev libsdl-ttf2.0-dev

And than Make:
- sudo make
